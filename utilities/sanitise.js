export const sanitiseEmail = function (email) {
  return email.trim();
};

export const sanitisePassword = function (password) {
  return password.trim();
};

export const sanitiseName = function (name) {
  return name.trim();
};
